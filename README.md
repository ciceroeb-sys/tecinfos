# tecnoinfos
Informática e Segurança - ADS - TI

### Instruções de Inicialização:
```bash
git init
git add README.md
git commit -m "primeiro commit"
git branch -M main
git remote add origin https://github.com
git push -u origin main
